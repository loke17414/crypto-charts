{
  "name": "crypto-charts-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Headless 24/7 futures bot running the exact CryptoCharts strategy engine",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "sync": "node sync-strategy.js",
    "check": "node --check bot.js && node --check binance.js && node --check config.js && node --check strategy-runtime.js && node --check sync-strategy.js"
  },
  "engines": {
    "node": ">=18"
  }
}
